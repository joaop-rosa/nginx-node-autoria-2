### Nginx com Node e express

O projeto utiliza `Docker`para subir um container com `Node` e um com `NGINX` e assim poder fornecer um curriculo em `http://localhost/`

##### Como rodar o projeto:

`docker-compose up --build`
